#!/usr/bin/env bash
set -euo pipefail

# Phase 4 stub. Intentionally empty — populate with real `defaults write`
# commands captured via prefsniff during a discovery session, not guessed
# or copied from someone else's dotfiles.
